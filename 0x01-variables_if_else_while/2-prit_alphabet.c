#include <stdio.h>
/**
 * main - entry point
 *
 * Desription: Print the lowercase alphabet
 * Return: 0
 */
int main(void)
{
int c;

for (c = 0; c < 26, ++c)
putchar('a' + c);

putchr('\n');

return (0);
}
